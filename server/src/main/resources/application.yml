server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://database-1.cj0owo8uehby.us-west-1.rds.amazonaws.com:3306/lms_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: admin
    password: system32
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jackson:
    time-zone: UTC

app:
  jwt:
    secret: "change-this-super-secret-key-please"
    expiration-minutes: 240
  seed:
    admin-email: admin@secforgtek.com
    admin-password: Admin123!

springdoc:
  swagger-ui:
    path: /swagger-ui.html
