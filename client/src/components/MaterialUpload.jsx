export default function MaterialUpload() {
  return (
    <div className="stack">
      <p>Upload PDF, video, or PPT files for students. (Demo UI only)</p>
      <input type="file" />
      <button className="ghost" type="button">Upload</button>
    </div>
  );
}
